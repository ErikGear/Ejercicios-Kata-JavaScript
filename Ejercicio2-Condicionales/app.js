// 2. Divisibilidad de un numero

const number = parseInt(prompt("Ingrse un numero"));

if (number % 2 === 0) {
    alert(`El numero ${number} es divisible por 2`);
} else {
    alert(`El numero ${number} no es divisible por 2`);
}