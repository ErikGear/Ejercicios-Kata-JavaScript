
const numeroGanador = parseInt(prompt("Digite un numero"));

if (numeroGanador === 1000) {
    alert(`El numero es ${numeroGanador} Ganaste un premio`);
} else {
 alert(`El numero es ${numeroGanador} Lo sentimos siga participando`);   
}